note: i started this late so it def doesn't have everything

- dropped location entity type
- made trip pk just trip_id
- charges got its own id
- added "type" for user, electric_vehicle
- added electric vehicle id to trip
- dropped fuel_used
- dropped bank_account (using an "external payment processor" instead)